name: go test

permissions:
  pull-requests: read
  contents: read

on:
  push:
    branches:
      - master

  pull_request:

jobs:
  test:
    uses: go-openapi/ci-workflows/.github/workflows/go-test.yml@d87ca69d0f0464d70cdece61c88919725a3571a2 # v0.4.4
    secrets: inherit
