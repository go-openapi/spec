name: Release on tag

permissions:
  contents: read

on:
  push:
    tags:
      - v[0-9]+*

jobs:
  gh-release:
    name: Create release
    permissions:
      contents: write
    uses: go-openapi/ci-workflows/.github/workflows/release.yml@cba06a2e6390abfedda6bf1067f44741cbe93650 # v0.6.2
    with:
      tag: ${{ github.ref_name }}
    secrets: inherit
